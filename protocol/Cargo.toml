[package]
name = "vxapo-protocol"
version = "0.3.0"
edition = "2021"
description = "VxAPO CLI --json 输出契约（CLI 与 App 共用的纯数据结构）"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ts-rs = "12"
